# FastAPI backend base URL
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Explicit mock switch
# true  -> use local mock data in the frontend
# false -> call the real FastAPI backend
NEXT_PUBLIC_MOCK=false
